# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_code.py

test "CodeTest.test_closure_injection" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nfrom opcode import opmap, opname\nCOPY_FREE_VARS = opmap['COPY_FREE_VARS']\n\ndef external_getitem(self, i):\n    return f'Foreign getitem: {super().__getitem__(i)}'\n\ndef _t():\n    from types import FunctionType\n\n    def create_closure(__class__):\n        return (lambda: __class__).__closure__\n\n    def new_code(c):\n        \"\"\"A new code object with a __class__ cell added to freevars\"\"\"\n        return c.replace(co_freevars=c.co_freevars + ('__class__',), co_code=bytes([COPY_FREE_VARS, 1]) + c.co_code)\n\n    def add_foreign_method(cls, name, f):\n        code = new_code(f.__code__)\n        assert not f.__closure__\n        closure = create_closure(cls)\n        defaults = f.__defaults__\n        setattr(cls, name, FunctionType(code, globals(), name, defaults, closure))\n\n    class List(list):\n        pass\n    add_foreign_method(List, '__getitem__', external_getitem)\n    function = List.__getitem__\n    class_ref = function.__closure__[0].cell_contents\n    assert class_ref is List, ('assertIs', class_ref, List)\n    obj = List([1, 2, 3])\n    assert obj[0] == 'Foreign getitem: 1', ('assertEqual', obj[0], 'Foreign getitem: 1')\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_constructor" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n\n    def func():\n        pass\n    co = func.__code__\n    CodeType = type(co)\n    CodeType(co.co_argcount, co.co_posonlyargcount, co.co_kwonlyargcount, co.co_nlocals, co.co_stacksize, co.co_flags, co.co_code, co.co_consts, co.co_names, co.co_varnames, co.co_filename, co.co_name, co.co_qualname, co.co_firstlineno, co.co_linetable, co.co_exceptiontable, co.co_freevars, co.co_cellvars)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_shrinking_localsplus" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n\n    def func(arg):\n        return lambda: arg\n    code = func.__code__\n    newcode = code.replace(co_name='func')\n    assert code == newcode, ('assertEqual', code, newcode)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_empty_linetable" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n\n    def func():\n        pass\n    new_code = code = func.__code__.replace(co_linetable=b'')\n    assert list(new_code.co_lines()) == [], ('assertEqual', list(new_code.co_lines()), [])\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_co_positions_empty_linetable" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n\n    def func():\n        x = 1\n    new_code = func.__code__.replace(co_linetable=b'')\n    positions = new_code.co_positions()\n    for line, end_line, column, end_column in positions:\n        assert line is None, ('assertIsNone', line)\n        assert end_line == new_code.co_firstlineno + 1, ('assertEqual', end_line, new_code.co_firstlineno + 1)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_code_hash_uses_firstlineno" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n    c1 = (lambda: 1).__code__\n    c2 = (lambda: 1).__code__\n    assert c1 != c2, ('assertNotEqual', c1, c2)\n    assert hash(c1) != hash(c2), ('assertNotEqual', hash(c1), hash(c2))\n    c3 = c1.replace(co_firstlineno=17)\n    assert c1 != c3, ('assertNotEqual', c1, c3)\n    assert hash(c1) != hash(c3), ('assertNotEqual', hash(c1), hash(c3))\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_code_hash_uses_order" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n    c = (lambda x, y, *, z=1, w=1: 1).__code__\n    assert c.co_argcount == 2, ('assertEqual', c.co_argcount, 2)\n    assert c.co_posonlyargcount == 0, ('assertEqual', c.co_posonlyargcount, 0)\n    assert c.co_kwonlyargcount == 2, ('assertEqual', c.co_kwonlyargcount, 2)\n    swapped = c.replace(co_posonlyargcount=2, co_kwonlyargcount=0)\n    assert c != swapped, ('assertNotEqual', c, swapped)\n    assert hash(c) != hash(swapped), ('assertNotEqual', hash(c), hash(swapped))\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_code_hash_uses_bytecode" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n    c = (lambda x, y: x + y).__code__\n    d = (lambda x, y: x * y).__code__\n    c1 = c.replace(co_code=d.co_code)\n    assert c != c1, ('assertNotEqual', c, c1)\n    assert hash(c) != hash(c1), ('assertNotEqual', hash(c), hash(c1))\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeTest.test_code_equal_with_instrumentation" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef _t():\n    \"\"\" GH-109052\n\n        Make sure the instrumentation doesn't affect the code equality\n        The validity of this test relies on the fact that \"x is x\" and\n        \"x in x\" have only one different instruction and the instructions\n        have the same argument.\n\n        \"\"\"\n    code1 = compile('x is x', 'example.py', 'eval')\n    code2 = compile('x in x', 'example.py', 'eval')\n    sys._getframe().f_trace_opcodes = True\n    sys.settrace(lambda *args: None)\n    exec(code1, {'x': []})\n    exec(code2, {'x': []})\n    assert code1 != code2, ('assertNotEqual', code1, code2)\n    sys.settrace(None)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_interned_string" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef consts(t):\n    \"\"\"Yield a doctest-safe sequence of object reprs.\"\"\"\n    for elt in t:\n        r = repr(elt)\n        if r.startswith('<code object'):\n            yield ('<code object %s>' % elt.co_name)\n        else:\n            yield r\n\ndef isinterned(s):\n    return s is sys.intern(('_' + s + '_')[1:-1])\n\ndef _t():\n\n    def find_const(consts, value):\n        for v in consts:\n            if v == value:\n                return v\n        assert value in consts, ('assertIn', value, consts)\n        raise AssertionError('Should never be reached')\n\n    def assertIsInterned(s):\n        if not isinterned(s):\n            raise AssertionError('String %r is not interned' % (s,))\n    co = compile('res = \"str_value\"', '?', 'exec')\n    v = find_const(co.co_consts, 'str_value')\n    assertIsInterned(v)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_interned_string_in_tuple" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef consts(t):\n    \"\"\"Yield a doctest-safe sequence of object reprs.\"\"\"\n    for elt in t:\n        r = repr(elt)\n        if r.startswith('<code object'):\n            yield ('<code object %s>' % elt.co_name)\n        else:\n            yield r\n\ndef isinterned(s):\n    return s is sys.intern(('_' + s + '_')[1:-1])\n\ndef _t():\n\n    def find_const(consts, value):\n        for v in consts:\n            if v == value:\n                return v\n        assert value in consts, ('assertIn', value, consts)\n        raise AssertionError('Should never be reached')\n\n    def assertIsInterned(s):\n        if not isinterned(s):\n            raise AssertionError('String %r is not interned' % (s,))\n    co = compile('res = (\"str_value\",)', '?', 'exec')\n    v = find_const(co.co_consts, ('str_value',))\n    assertIsInterned(v[0])\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_interned_string_in_frozenset" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef consts(t):\n    \"\"\"Yield a doctest-safe sequence of object reprs.\"\"\"\n    for elt in t:\n        r = repr(elt)\n        if r.startswith('<code object'):\n            yield ('<code object %s>' % elt.co_name)\n        else:\n            yield r\n\ndef isinterned(s):\n    return s is sys.intern(('_' + s + '_')[1:-1])\n\ndef _t():\n\n    def find_const(consts, value):\n        for v in consts:\n            if v == value:\n                return v\n        assert value in consts, ('assertIn', value, consts)\n        raise AssertionError('Should never be reached')\n\n    def assertIsInterned(s):\n        if not isinterned(s):\n            raise AssertionError('String %r is not interned' % (s,))\n    co = compile('res = a in {\"str_value\"}', '?', 'exec')\n    v = find_const(co.co_consts, frozenset(('str_value',)))\n    assertIsInterned(tuple(v)[0])\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_interned_string_default" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef isinterned(s):\n    return s is sys.intern(('_' + s + '_')[1:-1])\n\ndef _t():\n\n    def assertIsInterned(s):\n        if not isinterned(s):\n            raise AssertionError('String %r is not interned' % (s,))\n\n    def f(a='str_value'):\n        return a\n    assertIsInterned(f())\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_interned_string_with_null" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\nimport sys\n\ndef consts(t):\n    \"\"\"Yield a doctest-safe sequence of object reprs.\"\"\"\n    for elt in t:\n        r = repr(elt)\n        if r.startswith('<code object'):\n            yield ('<code object %s>' % elt.co_name)\n        else:\n            yield r\n\ndef isinterned(s):\n    return s is sys.intern(('_' + s + '_')[1:-1])\n\ndef _t():\n\n    def find_const(consts, value):\n        for v in consts:\n            if v == value:\n                return v\n        assert value in consts, ('assertIn', value, consts)\n        raise AssertionError('Should never be reached')\n\n    def assertIsNotInterned(s):\n        if isinterned(s):\n            raise AssertionError('String %r is interned' % (s,))\n    co = compile('res = \"str\\\\0value!\"', '?', 'exec')\n    v = find_const(co.co_consts, 'str\\x00value!')\n    assertIsNotInterned(v)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeConstsTest.test_unusual_constants" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef _t():\n\n    class Unhashable:\n\n        def __hash__(self):\n            raise TypeError('unhashable type')\n\n    class MyInt(int):\n        pass\n    code = compile('a = 1', '<string>', 'exec')\n    code = code.replace(co_consts=(1, Unhashable(), MyInt(1), MyInt(1)))\n    assert isinstance(code.co_consts[1], Unhashable), ('assertIsInstance', code.co_consts[1], Unhashable)\n    assert code.co_consts[2] == code.co_consts[3], ('assertEqual', code.co_consts[2], code.co_consts[3])\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeLocationTest.test_positions" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef read(it):\n    return next(it)\n\ndef read_varint(it):\n    b = read(it)\n    val = b & 63\n    shift = 0\n    while b & 64:\n        b = read(it)\n        shift += 6\n        val |= (b & 63) << shift\n    return val\n\ndef read_signed_varint(it):\n    uval = read_varint(it)\n    if uval & 1:\n        return -(uval >> 1)\n    else:\n        return uval >> 1\n\ndef parse_location_table(code):\n    line = code.co_firstlineno\n    it = iter(code.co_linetable)\n    while True:\n        try:\n            first_byte = read(it)\n        except StopIteration:\n            return\n        code = first_byte >> 3 & 15\n        length = (first_byte & 7) + 1\n        if code == 15:\n            yield (code, length, None, None, None, None)\n        elif code == 14:\n            line_delta = read_signed_varint(it)\n            line += line_delta\n            end_line = line + read_varint(it)\n            col = read_varint(it)\n            if col == 0:\n                col = None\n            else:\n                col -= 1\n            end_col = read_varint(it)\n            if end_col == 0:\n                end_col = None\n            else:\n                end_col -= 1\n            yield (code, length, line, end_line, col, end_col)\n        elif code == 13:\n            line_delta = read_signed_varint(it)\n            line += line_delta\n            yield (code, length, line, line, None, None)\n        elif code in (10, 11, 12):\n            line_delta = code - 10\n            line += line_delta\n            column = read(it)\n            end_column = read(it)\n            yield (code, length, line, line, column, end_column)\n        else:\n            assert 0 <= code < 10\n            second_byte = read(it)\n            column = code << 3 | second_byte >> 4\n            yield (code, length, line, line, column, column + (second_byte & 15))\n\ndef positions_from_location_table(code):\n    for _, length, line, end_line, col, end_col in parse_location_table(code):\n        for _ in range(length):\n            yield (line, end_line, col, end_col)\n\ndef misshappen():\n    \"\"\"\n\n\n\n\n\n    \"\"\"\n    x = 4 + y\n    y = a + b + d\n    return q if x else p\n\ndef bug93662():\n    example_report_generation_message = '\\n            '.strip()\n    raise ValueError()\n\ndef _t():\n\n    def check_positions(func):\n        pos1 = list(func.__code__.co_positions())\n        pos2 = list(positions_from_location_table(func.__code__))\n        for l1, l2 in zip(pos1, pos2):\n            assert l1 == l2, ('assertEqual', l1, l2)\n        assert len(pos1) == len(pos2), ('assertEqual', len(pos1), len(pos2))\n    check_positions(parse_location_table)\n    check_positions(misshappen)\n    check_positions(bug93662)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "CodeLocationTest.test_lines" {
    (ok, detail) = p2_libtest_run_snippet("import os as _os\n\ndef _oracle_write(text):\n    _os.write(1, (text + '\\n').encode())\n\ndef read(it):\n    return next(it)\n\ndef read_varint(it):\n    b = read(it)\n    val = b & 63\n    shift = 0\n    while b & 64:\n        b = read(it)\n        shift += 6\n        val |= (b & 63) << shift\n    return val\n\ndef read_signed_varint(it):\n    uval = read_varint(it)\n    if uval & 1:\n        return -(uval >> 1)\n    else:\n        return uval >> 1\n\ndef parse_location_table(code):\n    line = code.co_firstlineno\n    it = iter(code.co_linetable)\n    while True:\n        try:\n            first_byte = read(it)\n        except StopIteration:\n            return\n        code = first_byte >> 3 & 15\n        length = (first_byte & 7) + 1\n        if code == 15:\n            yield (code, length, None, None, None, None)\n        elif code == 14:\n            line_delta = read_signed_varint(it)\n            line += line_delta\n            end_line = line + read_varint(it)\n            col = read_varint(it)\n            if col == 0:\n                col = None\n            else:\n                col -= 1\n            end_col = read_varint(it)\n            if end_col == 0:\n                end_col = None\n            else:\n                end_col -= 1\n            yield (code, length, line, end_line, col, end_col)\n        elif code == 13:\n            line_delta = read_signed_varint(it)\n            line += line_delta\n            yield (code, length, line, line, None, None)\n        elif code in (10, 11, 12):\n            line_delta = code - 10\n            line += line_delta\n            column = read(it)\n            end_column = read(it)\n            yield (code, length, line, line, column, end_column)\n        else:\n            assert 0 <= code < 10\n            second_byte = read(it)\n            column = code << 3 | second_byte >> 4\n            yield (code, length, line, line, column, column + (second_byte & 15))\n\ndef positions_from_location_table(code):\n    for _, length, line, end_line, col, end_col in parse_location_table(code):\n        for _ in range(length):\n            yield (line, end_line, col, end_col)\n\ndef dedup(lst, prev=object()):\n    for item in lst:\n        if item != prev:\n            yield item\n            prev = item\n\ndef lines_from_postions(positions):\n    return dedup((l for l, _, _, _ in positions))\n\ndef misshappen():\n    \"\"\"\n\n\n\n\n\n    \"\"\"\n    x = 4 + y\n    y = a + b + d\n    return q if x else p\n\ndef bug93662():\n    example_report_generation_message = '\\n            '.strip()\n    raise ValueError()\n\ndef _t():\n\n    def check_lines(func):\n        co = func.__code__\n        lines1 = [line for _, _, line in co.co_lines()]\n        assert lines1 == list(dedup(lines1)), ('assertEqual', lines1, list(dedup(lines1)))\n        lines2 = list(lines_from_postions(positions_from_location_table(co)))\n        for l1, l2 in zip(lines1, lines2):\n            assert l1 == l2, ('assertEqual', l1, l2)\n        assert len(lines1) == len(lines2), ('assertEqual', len(lines1), len(lines2))\n    check_lines(parse_location_table)\n    check_lines(misshappen)\n    check_lines(bug93662)\ntry:\n    _t()\nexcept BaseException as _e:\n    _oracle_write('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    _oracle_write('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
