# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_itertools.py

test "TestBasicOps.test_tee_dealloc_segfault" {
    (ok, detail) = p2_libtest_run_snippet("from test.support import threading_helper, script_helper\n\ndef _t():\n    script = 'import typing, copyreg, itertools; copyreg.buggy_tee = itertools.tee(())'\n    script_helper.assert_python_ok('-c', script)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestExamples.test_filter" {
    (ok, detail) = p2_libtest_run_snippet("import operator\nfrom functools import reduce\n\ndef onearg(x):\n    \"\"\"Test function of one argument\"\"\"\n    return 2 * x\n\ndef gen3():\n    \"\"\"Non-restartable source sequence\"\"\"\n    for i in (0, 1, 2):\n        yield i\n\ndef isEven(x):\n    \"\"\"Test predicate\"\"\"\n    return x % 2 == 0\n\ndef isOdd(x):\n    \"\"\"Test predicate\"\"\"\n    return x % 2 == 1\n\ndef irange(n):\n    for i in range(n):\n        yield i\n\ndef take(n, seq):\n    \"\"\"Convenience function for partially consuming a long of infinite iterable\"\"\"\n    return list(islice(seq, n))\n\ndef prod(iterable):\n    return reduce(operator.mul, iterable, 1)\n\ndef fact(n):\n    \"\"\"Factorial\"\"\"\n    return prod(range(1, n + 1))\n\ndef underten(x):\n    return x < 10\n\ndef R(seqn):\n    \"\"\"Regular generator\"\"\"\n    for i in seqn:\n        yield i\n\ndef L(seqn):\n    \"\"\"Test multiple tiers of iterators\"\"\"\n    return chain(map(lambda x: x, R(Ig(G(seqn)))))\n\ndef _t():\n    assert list(filter(lambda x: x % 2, range(10))) == [1, 3, 5, 7, 9], ('assertEqual', list(filter(lambda x: x % 2, range(10))), [1, 3, 5, 7, 9])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestExamples.test_map" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    assert list(map(pow, (2, 3, 10), (5, 2, 3))) == [32, 9, 1000], ('assertEqual', list(map(pow, (2, 3, 10), (5, 2, 3))), [32, 9, 1000])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestExamples.test_zip" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    assert list(zip('ABCD', 'xy')) == [('A', 'x'), ('B', 'y')], ('assertEqual', list(zip('ABCD', 'xy')), [('A', 'x'), ('B', 'y')])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestGC.test_filter" {
    (ok, detail) = p2_libtest_run_snippet("def makecycle(iterator, container):\n    container.append(iterator)\n    next(iterator)\n    del container, iterator\n\ndef _t():\n    a = []\n    makecycle(filter(lambda x: True, [a] * 2), a)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestGC.test_zip" {
    (ok, detail) = p2_libtest_run_snippet("def makecycle(iterator, container):\n    container.append(iterator)\n    next(iterator)\n    del container, iterator\n\ndef _t():\n    a = []\n    makecycle(zip([a] * 2, [a] * 3), a)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestGC.test_map" {
    (ok, detail) = p2_libtest_run_snippet("import operator\nfrom functools import reduce\n\ndef onearg(x):\n    \"\"\"Test function of one argument\"\"\"\n    return 2 * x\n\ndef gen3():\n    \"\"\"Non-restartable source sequence\"\"\"\n    for i in (0, 1, 2):\n        yield i\n\ndef isEven(x):\n    \"\"\"Test predicate\"\"\"\n    return x % 2 == 0\n\ndef isOdd(x):\n    \"\"\"Test predicate\"\"\"\n    return x % 2 == 1\n\ndef irange(n):\n    for i in range(n):\n        yield i\n\ndef take(n, seq):\n    \"\"\"Convenience function for partially consuming a long of infinite iterable\"\"\"\n    return list(islice(seq, n))\n\ndef prod(iterable):\n    return reduce(operator.mul, iterable, 1)\n\ndef fact(n):\n    \"\"\"Factorial\"\"\"\n    return prod(range(1, n + 1))\n\ndef underten(x):\n    return x < 10\n\ndef R(seqn):\n    \"\"\"Regular generator\"\"\"\n    for i in seqn:\n        yield i\n\ndef L(seqn):\n    \"\"\"Test multiple tiers of iterators\"\"\"\n    return chain(map(lambda x: x, R(Ig(G(seqn)))))\n\ndef makecycle(iterator, container):\n    container.append(iterator)\n    next(iterator)\n    del container, iterator\n\ndef _t():\n    a = []\n    makecycle(map(lambda x: x, [a] * 2), a)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "RegressionTests.test_sf_793826" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def mutatingtuple(tuple1, f, tuple2):\n\n        def g(value, first=[1]):\n            if first:\n                del first[:]\n                f(next(z))\n            return value\n        items = list(tuple2)\n        items[1:1] = list(tuple1)\n        gen = map(g, items)\n        z = zip(*[gen] * len(tuple1))\n        next(z)\n\n    def f(t):\n        global T\n        T = t\n        first[:] = list(T)\n    first = []\n    mutatingtuple((1, 2, 3), f, (4, 5, 6))\n    second = list(T)\n    assert first == second, ('assertEqual', first, second)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
