# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_wave.py

test "WaveLowLevelTest.test_read_no_chunks" {
    (ok, detail) = p2_libtest_run_snippet("import io\nimport wave\n\ndef _t():\n    b = b'SPAM'\n    try:\n        wave.open(io.BytesIO(b))\n    except EOFError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_no_riff_chunk" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'SPAM' + struct.pack('<L', 0)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('file does not start with RIFF id', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_not_wave" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 4) + b'SPAM'\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('not a WAVE file', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_no_fmt_no_data_chunk" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 4) + b'WAVE'\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('fmt chunk and/or data chunk missing', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_no_data_chunk" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 28) + b'WAVE'\n    b += b'fmt ' + struct.pack('<LHHLLHH', 16, 1, 1, 11025, 11025, 1, 8)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('fmt chunk and/or data chunk missing', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_no_fmt_chunk" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 12) + b'WAVE'\n    b += b'data' + struct.pack('<L', 0)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('data chunk before fmt chunk', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_wrong_form" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 36) + b'WAVE'\n    b += b'fmt ' + struct.pack('<LHHLLHH', 16, 2, 1, 11025, 11025, 1, 1)\n    b += b'data' + struct.pack('<L', 0)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('unknown format: 2', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_wrong_number_of_channels" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 36) + b'WAVE'\n    b += b'fmt ' + struct.pack('<LHHLLHH', 16, 1, 0, 11025, 11025, 1, 8)\n    b += b'data' + struct.pack('<L', 0)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('bad # of channels', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_read_wrong_sample_width" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport io\nimport struct\nimport wave\n\ndef _t():\n    b = b'RIFF' + struct.pack('<L', 36) + b'WAVE'\n    b += b'fmt ' + struct.pack('<LHHLLHH', 16, 1, 1, 11025, 11025, 1, 0)\n    b += b'data' + struct.pack('<L', 0)\n    try:\n        wave.open(io.BytesIO(b))\n    except wave.Error as _exc:\n        assert _re.search('bad sample width', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "WaveLowLevelTest.test_open_in_write_raises" {
    (ok, detail) = p2_libtest_run_snippet("from test import support\nimport os\nimport wave\n\ndef _t():\n    with support.catch_unraisable_exception() as cm:\n        try:\n            wave.open(os.curdir, 'wb')\n        except OSError as _exc:\n            pass\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        support.gc_collect()\n        assert cm.unraisable is None, ('assertIsNone', cm.unraisable)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
