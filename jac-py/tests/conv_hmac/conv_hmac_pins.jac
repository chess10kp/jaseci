# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_hmac.py

test "PythonCopyTestCase.test_attributes" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\n\ndef _t():\n    h1 = hmac.HMAC.__new__(hmac.HMAC)\n    h1._init_old(b'key', b'msg', digestmod='sha256')\n    assert h1._hmac is None, ('assertIsNone', h1._hmac)\n    assert h1._inner is not None, ('assertIsNotNone', h1._inner)\n    assert h1._outer is not None, ('assertIsNotNone', h1._outer)\n    h2 = h1.copy()\n    assert h2._hmac is None, ('assertIsNone', h2._hmac)\n    assert h2._inner is not None, ('assertIsNotNone', h2._inner)\n    assert h2._outer is not None, ('assertIsNotNone', h2._outer)\n    assert type(h1._inner) == type(h2._inner), ('assertEqual', type(h1._inner), type(h2._inner))\n    assert type(h1._outer) == type(h2._outer), ('assertEqual', type(h1._outer), type(h2._outer))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "PythonCopyTestCase.test_realcopy" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\n\ndef _t():\n    h1 = hmac.HMAC.__new__(hmac.HMAC)\n    h1._init_old(b'key', b'msg', digestmod='sha256')\n    h2 = h1.copy()\n    assert id(h1) != id(h2), ('assertNotEqual', id(h1), id(h2))\n    assert id(h1._inner) != id(h2._inner), ('assertNotEqual', id(h1._inner), id(h2._inner))\n    assert id(h1._outer) != id(h2._outer), ('assertNotEqual', id(h1._outer), id(h2._outer))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "PythonCopyTestCase.test_equality" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\n\ndef _t():\n    h1 = hmac.HMAC(b'key', digestmod='sha256')\n    h1.update(b'some random text')\n    h2 = h1.copy()\n    assert h1.digest() == h2.digest(), ('assertEqual', h1.digest(), h2.digest())\n    assert h1.hexdigest() == h2.hexdigest(), ('assertEqual', h1.hexdigest(), h2.hexdigest())\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "PythonCopyTestCase.test_equality_new" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\n\ndef _t():\n    h1 = hmac.new(b'key', digestmod='sha256')\n    h1.update(b'some random text')\n    h2 = h1.copy()\n    assert id(h1) != id(h2), ('assertNotEqual', id(h1), id(h2))\n    assert h1.digest() == h2.digest(), ('assertEqual', h1.digest(), h2.digest())\n    assert h1.hexdigest() == h2.hexdigest(), ('assertEqual', h1.hexdigest(), h2.hexdigest())\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "PyMiscellaneousTests.test_hmac_delegated_properties" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\n\ndef _t():\n    h = hmac.HMAC(b'key', b'msg', digestmod='sha256')\n    assert h.name == 'hmac-sha256', ('assertEqual', h.name, 'hmac-sha256')\n    assert h.digest_size == 32, ('assertEqual', h.digest_size, 32)\n    assert h.block_size == 64, ('assertEqual', h.block_size, 64)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "PyMiscellaneousTests.test_with_fallback" {
    (ok, detail) = p2_libtest_run_snippet("import hmac\nimport hashlib\n\ndef _t():\n    cache = getattr(hashlib, '__builtin_constructor_cache')\n    try:\n        cache['foo'] = hashlib.sha256\n        hexdigest = hmac.digest(b'key', b'message', 'foo').hex()\n        expected = '6e9ef29b75fffc5b7abae527d58fdadb2fe42e7219011976917343065f58ed4a'\n        assert hexdigest == expected, ('assertEqual', hexdigest, expected)\n    finally:\n        cache.pop('foo')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
