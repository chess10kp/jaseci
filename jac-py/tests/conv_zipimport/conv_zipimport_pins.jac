# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_zipimport.py

test "BadFileZipImportTestCase.testNoFile" {
    (ok, detail) = p2_libtest_run_snippet("import zipimport\n\ndef assertZipFailure(filename):\n    try:\n        zipimport.zipimporter(*(filename,))\n    except zipimport.ZipImportError:\n        pass\n\ndef _t():\n    assertZipFailure('AdfjdkFJKDFJjdklfjs')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "BadFileZipImportTestCase.testEmptyFilename" {
    (ok, detail) = p2_libtest_run_snippet("import zipimport\n\ndef assertZipFailure(filename):\n    try:\n        zipimport.zipimporter(*(filename,))\n    except zipimport.ZipImportError:\n        pass\n\ndef _t():\n    assertZipFailure('')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "BadFileZipImportTestCase.testBadArgs" {
    (ok, detail) = p2_libtest_run_snippet("import os\nimport zipimport\nTESTMOD = 'ziptestmodule'\n\ndef _t():\n    try:\n        zipimport.zipimporter(*(None,))\n    except TypeError:\n        pass\n    try:\n        zipimport.zipimporter(*(TESTMOD,), kwd=None)\n    except TypeError:\n        pass\n    try:\n        zipimport.zipimporter(*(list(os.fsencode(TESTMOD)),))\n    except TypeError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "BadFileZipImportTestCase.testFilenameTooLong" {
    (ok, detail) = p2_libtest_run_snippet("import zipimport\n\ndef assertZipFailure(filename):\n    try:\n        zipimport.zipimporter(*(filename,))\n    except zipimport.ZipImportError:\n        pass\n\ndef _t():\n    assertZipFailure('A' * 33000)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "BadFileZipImportTestCase.testEmptyFile" {
    (ok, detail) = p2_libtest_run_snippet("from test.support import os_helper\nimport zipimport\nTESTMOD = 'ziptestmodule'\n\ndef assertZipFailure(filename):\n    try:\n        zipimport.zipimporter(*(filename,))\n    except zipimport.ZipImportError:\n        pass\n\ndef _t():\n    os_helper.unlink(TESTMOD)\n    os_helper.create_empty_file(TESTMOD)\n    assertZipFailure(TESTMOD)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "BadFileZipImportTestCase.testNotZipFile" {
    (ok, detail) = p2_libtest_run_snippet("from test.support import os_helper\nimport zipimport\nTESTMOD = 'ziptestmodule'\n\ndef assertZipFailure(filename):\n    try:\n        zipimport.zipimporter(*(filename,))\n    except zipimport.ZipImportError:\n        pass\n\ndef _t():\n    os_helper.unlink(TESTMOD)\n    fp = open(TESTMOD, 'w+')\n    fp.write('a' * 22)\n    fp.close()\n    assertZipFailure(TESTMOD)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
