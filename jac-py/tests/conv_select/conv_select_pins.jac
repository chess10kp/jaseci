# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_select.py

test "SelectTestCase.test_error_conditions" {
    (ok, detail) = p2_libtest_run_snippet("import select\n\nclass Nope:\n    pass\n\nclass Almost:\n\n    def fileno(self):\n        return 'fileno'\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    self.Almost = Almost\n    _SelfNS.Almost = Almost\n    self.Nope = Nope\n    _SelfNS.Nope = Nope\n    try:\n        select.select(*(1, 2, 3))\n    except TypeError:\n        pass\n    try:\n        select.select(*([self.Nope()], [], []))\n    except TypeError:\n        pass\n    try:\n        select.select(*([self.Almost()], [], []))\n    except TypeError:\n        pass\n    try:\n        select.select(*([], [], [], 'not a number'))\n    except TypeError:\n        pass\n    try:\n        select.select(*([], [], [], -1))\n    except ValueError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "SelectTestCase.test_returned_list_identity" {
    (ok, detail) = p2_libtest_run_snippet("import select\n\ndef _t():\n    r, w, x = select.select([], [], [], 1)\n    assert r is not w, ('assertIsNot', r, w)\n    assert r is not x, ('assertIsNot', r, x)\n    assert w is not x, ('assertIsNot', w, x)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
