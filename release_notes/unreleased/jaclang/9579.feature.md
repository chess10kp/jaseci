- **Bundled Native  and **: Ports of CPython's pure-Python  (full TOML 1.0.0  with CPython-exact error messages and line/column positions) and  ( with BasicInterpolation, DEFAULTSECT fallback semantics, strict/non-strict duplicate handling, and the complete / error family) now run as bundled native modules — no Python required. tomllib parses ~2.1x faster than CPython 3.14 and configparser ~1.8x faster. Also speeds up native / string handling and  field building (~13%/20%) via chunked slice-appends, and adds  +  benchmark harnesses.
