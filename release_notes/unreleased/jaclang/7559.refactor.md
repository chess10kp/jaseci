- **Refactor: Dedup identical null-ok propagation in `exit_if_else_expr`/`exit_bool_expr`**: The duplicated inline trailer scan is extracted into a shared `_propagate_null_ok_siblings` helper. No behavior change.
