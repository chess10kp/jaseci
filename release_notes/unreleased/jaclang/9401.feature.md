Native `zlib.decompress` and `gzip.decompress` now stream libz `inflate` over a caller-owned `malloc`/`realloc` arena instead of restarting the one-shot API with doubling buffers, bringing decompression to within ~1.1x of CPython (previously ~2.6x slower), and `gzip.decompress` faster than CPython.
