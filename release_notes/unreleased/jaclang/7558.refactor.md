- **Refactor: Replace hand-rolled `_collect_atom_trailers` walk with cached `get_all_sub_nodes`**: The manual recursion is now a guard plus a single `get_all_sub_nodes(uni.AtomTrailer)` call. No behavior change.
