Bundled native `string`, `re` (a pure-Jac regex engine), and `reprlib` modules now resolve on the native pathway as Mechanism-B `na_stdlib` modules, each pinned sv<->na congruent by a new equivalence fixture (#6978 Phase 3).
