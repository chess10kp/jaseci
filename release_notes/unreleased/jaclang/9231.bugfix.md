- **Refactor: dedupe k8s deploy API boilerplate (#9231)**: inline read-or-create / patch-or-create / delete-if-exists blocks and hand-built `{apiVersion, kind, metadata}` manifests across the kubernetes deployers and autoscalers collapsed into shared helpers in `kubernetes_utils` (`apply_k8s_resource`, `patch_k8s_resource`, `delete_resource`, `delete_collection_if_exists`, `k8s_manifest`, label/security-context builders). `KubernetesTarget._apply_or_replace`'s stringly-typed method-name dispatch is gone; KEDA custom-object apply/list/delete now shares `_apply_custom_object`/`_list_custom_items`/`_delete_custom_object`. `monitoring.jac` 2396→1418 lines; no behavior change. Part of the simplification stack (#9227, #9229).
- **Refactor: endpoint-registration DSL (#9231)**: ~80 `server.add_endpoint(JEndPoint(method=..., path=..., callback=..., parameters=[APIParameter(...)...], response_model=None, ...))` blobs across `server/endpoints`, `server/serving`, and `scale/{admin,server}` now go through `ep()` plus `path_param`/`query_param`/`body_param`/`header_param` constructors in `server/serving/endpoint.jac` (optional-vs-required derives from a `_NO_DEFAULT` sentinel). Identical auth-binding handler stubs in the admin impls collapsed into `authed`/`authed_thread` wrappers (`scale/admin/auth_wrap.jac`), and uniform-tags registrations became route tables. `register_admin_endpoints` 497→~215 lines; net −456 LOC across 22 files; no behavior change.
