- **Feat(jac-py): band-3 control flow with parser and codegen fixes**: parser band-3 control-flow constructs (complex-literal lexing/typing/codegen included) plus matching codegen and test updates; CI gains the `jac-py-gates` lane.

Build the bundled CPython runtime and native dependency archives from checksum-pinned upstream sources using Zig, replacing python-build-standalone downloads. Cache the runtime independently of Jac compiler changes and exclude unsupported platform packaging, documentation, and CPython test sources. Source builds require make and Perl alongside Zig and run on matching Linux/macOS release runners.
