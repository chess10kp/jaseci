- **Build bundled Python from source with Zig**: Build CPython and its native dependencies from checksum-pinned sources, replacing python-build-standalone downloads. Cache the runtime by source and build inputs independently of Jac compiler changes. Validate regular Linux/macOS release targets in CI; Intel macOS remains in the manual release flow. Building requires make and Perl alongside Zig.
- **Organize CPython replacement work within Jac**: Place Python parsing and semantic analysis in the frontend, bytecode generation in the Python backend, and interpreter, object and module implementations in the runtime. Keep source generators under `scripts/python/`; remove bundled JacPython tests, fixtures, test helpers and their CI job. These implementations remain under development and do not yet replace the C runtime in release builds.
