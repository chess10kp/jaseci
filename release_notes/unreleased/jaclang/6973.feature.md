- **Build bundled Python from source with Zig**: Build CPython and its native dependencies from checksum-pinned sources, replacing python-build-standalone downloads. Cache the runtime by source and build inputs independently of Jac compiler changes. Validate regular Linux/macOS release targets in CI; Intel macOS remains in the manual release flow. Building requires make and Perl alongside Zig.
- **Add C interop tooling**: `jac c2jac` lifts C files and compilation databases into Jac with preprocessing, LP64 type mappings, fidelity diagnostics, and function quarantine. `jac cbindgen` and `clib(...)` imports generate C header bindings. `jac jac2c` supports C emission and differential round-trip tests.
- **Retain focused CPython replacement work**: Add the JacPython compiler, interpreter, object and module implementations with their regression tests and source generators. These implementations remain under development and do not yet replace the C runtime in release builds. Historical conversion corpora, agent tooling, and experimental scaffolding are excluded.
