- **Refactor: collapse god dispatch tables and dedupe parser helpers**: `unitree` edge helpers (`_attach`, `_role_clear`) were 210-branch per-role dispatch doing identical dynamic ops; replaced with 2-line generic impls. `parser_actions` 13 typed sequence helper pairs were copy-pasted loops; collapsed to one-liners and comprehensions. `roles` had 186 trivial `__repr__` overrides pruned. `opcode_meta` cache classifier is now table-driven. Net ~1.6k LOC deleted with no semantic change; phase 1 of multi-phase simplification toward 30% reduction.
