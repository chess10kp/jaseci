Native pure-Jac dependencies: declare git/path dependencies in `[dependencies.jac]`, install with `jac install jac:<url>[@ref]`, and import them directly. Packages resolve through a content-addressed store keyed by tree hashes, with flat conflict detection and `[override]` support.
