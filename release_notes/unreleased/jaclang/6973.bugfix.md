- **Correct C lifting and header binding**: Preserve initializer expressions and nested struct types, lower character/integer operations and chained assignments, and emit valid variadic bindings. Header changes invalidate cached bindings, and foreign imports round-trip through formatting.
- **Improve C emission correctness**: Use stable type identifiers, emit value-type constructors correctly, and unwind owned references across exception handling. Focused C/Jac differential tests cover the retained conversion paths.
- **Support Python replacement implementations**: Handle complex literals and lone-surrogate strings, preserve universal object introspection attributes, and prevent JacPython module names on `JACPATH` from shadowing the host standard library during compiler startup.
